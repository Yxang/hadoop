<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>hadoop-yarn</artifactId>
    <groupId>org.apache.hadoop</groupId>
    <version>3.1.1</version>
  </parent>

  <artifactId>hadoop-yarn-opera-extensions</artifactId>
  <name>Apache Hadoop YARN – Opera Extensions</name>
  <packaging>jar</packaging>

  <description>
    Custom extensions that enhance Apache Hadoop YARN functionality for Opera-specific
    requirements. The resulting shaded JAR will be added to the YARN runtime classpath so
    that the new classes are available without conflicting with existing ones.
  </description>

  <!-- Compile-time dependencies.  Add more as your extensions grow. -->
  <dependencies>
    <!-- Core YARN APIs that the extensions build upon -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-yarn-common</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-yarn-api</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- Bring in RM-side scheduler classes so we can implement custom placement algorithm -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-yarn-server-resourcemanager</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Provide org.apache.hadoop.test.TimedOutTestsListener and other common
         test utilities expected by the surefire configuration inherited from
         the parent POM -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-common</artifactId>
      <type>test-jar</type>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- Required for mocking RMContext interface (contains timeline manager types) -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-yarn-server-timelineservice</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- Test-only dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>3.12.4</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Override parent surefire configuration to remove Hadoop's custom
           TimedOutTestsListener, avoiding the need for the hadoop-common
           tests jar when building this standalone module. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <properties><!-- intentionally empty --></properties>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project> 